node_modules/
dist/
.vite/
backend/data/
backend/.mojing_secret
# Auth token + data-dir config — generated at runtime, must never ship
storage.json
**/storage.json
# PyInstaller build outputs (mojing-backend.exe etc.)
backend/build/
backend/dist/
墨境数据/
release/
__pycache__/
*.py[cod]
.venv/
.pytest_cache/

*.log
*.local

.edge-ui-*/
.DS_Store
Thumbs.db
# Local editor / agent tooling state — not part of the project
.claude/
.workbuddy/
.mimosa/
.zcode/
gui-test-screenshots/
# Local AI provider credentials (template lives at .env.example)
.env
.electron-dist/
墨境数据-imported-*/

# Generated by `npm run gen:types` (openapi-typescript from running backend)
src/api-types.ts

# Local UI screenshots / audit working dirs (curated copies live in docs/screenshots/)
/ui-*.png
ui-audit-shots/
