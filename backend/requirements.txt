fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sqlalchemy>=2.0.35
pydantic>=2.10.0
# Encryption for stored API keys (#1) and the OpenAI-compatible streaming
# provider's HTTP client. Both ship in the standard Python install the
# packaged build freezes, so they're listed explicitly here.
cryptography>=44.0.0
httpx>=0.27.0
# docx export (#5); pure-python, PyInstaller-friendly.
python-docx>=1.1.0
